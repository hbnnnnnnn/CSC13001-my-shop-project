<UserControl
    x:Class="CSC13001_my_shop_project.Presentation.Products.Controls.SummaryRowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:CSC13001_my_shop_project.Presentation.Products.Converters"
>
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <StackPanel Spacing="4">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                FontSize="11"
                FontWeight="SemiBold"
                Foreground="#FF9CA3AF"
                Text="{x:Bind Label, Mode=OneWay}"
            />
            <TextBlock
                Grid.Column="1"
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="#FF2C2118"
                Text="{x:Bind Value, Mode=OneWay}"
                TextAlignment="Right"
            />
        </Grid>
        <Border
            Height="1"
            Background="#FFF0EDE8"
            Visibility="{x:Bind ShowBottomDivider, Mode=OneWay, Converter={StaticResource BoolToVis}}"
        />
    </StackPanel>
</UserControl>
